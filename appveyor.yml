version: "{build}"

clone_folder: c:\gopath\src\github.com\Azure\acr-builder

# Build host

environment:
  GOPATH: c:\gopath
  GOVERSION: 1.9

install:
  - set Path=c:\go\bin;c:\gopath\bin;
  - go version
  - go env

build: false
deploy: false

test_script:
  - go build github.com/Azure/acr-builder
  - go build github.com/Azure/acr-builder/baseimages/scanner
  - go test ./...