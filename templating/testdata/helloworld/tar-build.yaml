# This template demonstrates a single-step build which builds from a tar.

steps:
  - run: build https://acrbuild.blob.core.windows.net/public/aspnetcore-helloworld.tar.gz -f HelloWorld/Dockerfile -t aspnetcore:{{.Build.ID}}
    rm: true
