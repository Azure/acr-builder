version: '2'
services:
  hello:
    build: ./hello-multistage
    image: ${ACR_BUILD_DOCKER_REGISTRY}/hello-builder

  hello-multistage:
    build: ./hello-node
    image: ${ACR_BUILD_DOCKER_REGISTRY}/hello-multistage
